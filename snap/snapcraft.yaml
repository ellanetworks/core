name: moose
base: core22
version: '0.0.0'
summary: Moose is a secure, reliable, and easy to operate mobile network.
description: |
  Moose is a secure, reliable, and easy to operate mobile network.

grade: devel
confinement: devmode

package-repositories:
  - type: apt
    components: [multiverse]
    suites: [jammy/mongodb-org/7.0]
    key-id: E58830201F7DD82CD808AA84160D26BB1785BA38
    url: https://repo.mongodb.org/apt/ubuntu

apps:
  moose:
    daemon: simple
    install-mode: disable
    command: bin/moose --config /etc/moose.yaml
    refresh-mode: endure
    restart-condition: on-failure

parts:
  moose:
    plugin: go
    source: .
    build-snaps:
      - go/1.22/stable
  
  mongodb:
    plugin: nil
    stage-packages:
      - mongodb-org
    stage:
      - usr/bin
      - usr/lib

  config:
    plugin: dump
    source: .
    organize:
      moose.yaml: etc/moose.yaml
    stage:
      - etc/moose.yaml

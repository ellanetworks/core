version: "2"
linters:
  enable:
    - asciicheck
    - bodyclose
    - dogsled
    - godox
    - gomodguard
    - misspell
    - nakedret
    - noctx
    - predeclared
    - staticcheck
    - unconvert
    - unparam
    - whitespace
    - govet
    - wsl_v5
  exclusions:
    # Installed by npm, not part of this repository.
    paths:
      - ui/node_modules
issues:
  new-from-rev: ""
  new: false
severity:
  default: error
formatters:
  enable:
    - gci
    - gofmt
    - gofumpt

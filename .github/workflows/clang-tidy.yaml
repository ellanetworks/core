name: Clang Tidy

permissions:
  contents: read

on:
  workflow_call:

jobs:
  lint:
    runs-on: ubuntu-26.04
    steps:
      - uses: actions/checkout@v7

      - name: Setup clang tidy
        run: |
          sudo apt update
          sudo apt install -y clang clang-tidy cmake llvm gcc-multilib libbpf-dev

      - name: Lint code
        run: |
          cd internal/upf/ebpf
          cmake .
          find bpf -name "*.[ch]" | xargs clang-tidy
